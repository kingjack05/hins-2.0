import { CollectionConfig } from 'payload/types'

export const ChiefComplaint: CollectionConfig = {
    slug: 'ChiefComplaint',
    fields: [
        {
            name: 'name',
            type: 'text',
        },
    ],
}

export default ChiefComplaint
